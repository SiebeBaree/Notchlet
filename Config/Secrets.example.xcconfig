// Copy to Secrets.xcconfig and fill in the PostHog project key.
// Optional: without it, analytics stays off, which is what you want for
// local development and contributor builds.

POSTHOG_API_KEY =
